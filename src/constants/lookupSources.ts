enum LookupSource {
  Api = 'api',
  DirectMessage = 'direct_message',
  ExistingLookup = 'existing_lookup',
  External = 'external',
  Manual = 'manual',
  Shortcuts = 'shortcuts',
  Status = 'status',
  WebClient = 'web_client',
}

export default LookupSource
